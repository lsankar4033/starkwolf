[package]
name = "cairo_vote"
version = "0.1.0"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest

[dependencies]
starknet = "2.0.1"

[[target.starknet-contract]]
sierra = true

[scripts]
test = "cairo-test --single-file ./src/verifier.cairo"